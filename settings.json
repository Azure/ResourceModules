{
    "parameterFileTokens": {
        "tokenPrefix": "<<",
        "tokenSuffix": ">>",
        "localTokens": {
            "namePrefix": "sxx" // A 3-5 character length string, included in the resources names
        }
    }
}
